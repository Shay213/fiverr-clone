import { FastifyInstance } from "fastify";

const BASE_API_URL = "/api/reviews";

export default function reviewRoute(
  fastify: FastifyInstance,
  options: object,
  done: () => void
) {
  done();
}
