import { FastifyInstance } from "fastify";

const BASE_API_URL = "/api/messages";

export default function messageRoute(
  fastify: FastifyInstance,
  options: object,
  done: () => void
) {
  done();
}
