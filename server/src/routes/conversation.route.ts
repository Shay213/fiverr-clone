import { FastifyInstance } from "fastify";

const BASE_API_URL = "/api/conversations";

export default function conversationRoute(
  fastify: FastifyInstance,
  options: object,
  done: () => void
) {
  done();
}
